<?xml version="1.0"?>
<doc>
    <assembly>
        <name>YDot.PAY.Api</name>
    </assembly>
    <members>
        <member name="T:YDot.PAY.Api.Controllers.ApiControllerBase">
             <summary>
             Shared base for every controller. It turns one <see cref="T:YDot.PAY.Application.Common.Results.Result"/> into the correct HTTP
             status code and wraps every payload in the same <c>ApiResponse</c> envelope, so success and
             failure look alike to the client and no action ever writes a status code by hand.
            
             THE ENVELOPE IS THE CONTRACT. Six keys, always present, on every response from every
             endpoint - and byte-for-byte the same shape IAM and DON return. That is what lets the
             Angular interceptor read <c>err.message</c> and <c>err.errorCode</c> without knowing which
             service it called.
            
             WHAT THIS REPLACES. Controllers used to build their own responses from a
             <c>Result&lt;T&gt;</c> that carried an <c>HttpStatusCode</c>, so the status came from
             whatever the handler author happened to think was right and the same kind of failure could
             answer 400 in one place and 409 in another. The status now comes from the error catalogue,
             through this one method.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ApiControllerBase.FromResult``1(YDot.PAY.Application.Common.Results.Result{``0},System.String)">
            <summary>200 OK with the value, or the mapped failure.</summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ApiControllerBase.CreatedFromResult``1(YDot.PAY.Application.Common.Results.Result{``0},System.String,System.Object,System.String)">
            <summary>201 Created with the value and a Location header, or the mapped failure.</summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ApiControllerBase.FileFromResult(YDot.PAY.Application.Common.Results.Result{YDot.PAY.Application.Common.Abstractions.Services.ExportFile})">
             <summary>
             A file download, with the audit reference in a response header.
            
             The header is what ties a spreadsheet found on somebody's desktop months later back to
             the audit row recording who exported it and when.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ApiControllerBase.Failure``1(YDot.PAY.Application.Common.Results.Error)">
             <summary>
             The single exit for every failure.
            
             One place means the status code always comes from the error catalogue rather than from
             whatever the action author happened to think was right, and the correlation id is never
             forgotten.
             </summary>
        </member>
        <member name="T:YDot.PAY.Api.Controllers.ChargebacksController">
             <summary>
             The chargeback register - SCR-PAY-008.
            
             UNLIKE A REFUND, NONE OF THIS IS OUR DECISION. The donor's bank has already taken the money
             back; the case exists so somebody can CONTEST it before the evidence deadline, and after that
             deadline nothing anybody does here changes the outcome.
            
             THAT IS WHY THE DEFAULT ORDERING IS BY URGENCY rather than by date, and why the list carries a
             server-computed <c>DaysUntilEvidenceDue</c>. A case with two days left matters more than one
             opened this morning with thirty, and leaving each client to work that out means they
             eventually disagree about which is which.
            
             THERE IS NO ENDPOINT TO OPEN A CASE BY HAND. Chargebacks arrive from the gateway, through the
             webhook and the event queue. A hand-created case would be an assertion that a bank did
             something, with nothing to corroborate it.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ChargebacksController.#ctor(YDot.PAY.Application.Features.Refunds.Queries.RefundQueryHandler,YDot.PAY.Application.Features.Refunds.Commands.ManageChargeback.ChargebackCommandHandler)">
             <summary>
             The chargeback register - SCR-PAY-008.
            
             UNLIKE A REFUND, NONE OF THIS IS OUR DECISION. The donor's bank has already taken the money
             back; the case exists so somebody can CONTEST it before the evidence deadline, and after that
             deadline nothing anybody does here changes the outcome.
            
             THAT IS WHY THE DEFAULT ORDERING IS BY URGENCY rather than by date, and why the list carries a
             server-computed <c>DaysUntilEvidenceDue</c>. A case with two days left matters more than one
             opened this morning with thirty, and leaving each client to work that out means they
             eventually disagree about which is which.
            
             THERE IS NO ENDPOINT TO OPEN A CASE BY HAND. Chargebacks arrive from the gateway, through the
             webhook and the event queue. A hand-created case would be an assertion that a bank did
             something, with nothing to corroborate it.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ChargebacksController.SearchAsync(YDot.PAY.Application.Features.Refunds.DTOs.ChargebackSearchFilter,System.Threading.CancellationToken)">
            <summary>The chargeback register, most urgent first.</summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ChargebacksController.GetChargebackAsync(System.Guid,System.Threading.CancellationToken)">
            <summary>One chargeback case in full, with its evidence.</summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ChargebacksController.AssignAsync(System.Guid,YDot.PAY.Application.Features.Refunds.DTOs.AssignChargebackRequest,System.Threading.CancellationToken)">
             <summary>
             Assigns the case to somebody.
            
             A CASE WITH NO OWNER IS ONE NOBODY WORKS, and this is the module where that has a deadline
             attached to it.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ChargebacksController.SubmitEvidenceAsync(System.Guid,YDot.PAY.Application.Features.Refunds.DTOs.SubmitChargebackEvidenceRequest,System.Threading.CancellationToken)">
             <summary>
             Submits evidence contesting the chargeback.
            
             REFUSED ONCE THE DEADLINE HAS PASSED, rather than accepted and quietly ignored. Letting
             somebody file evidence the bank will never look at leaves them believing the case is
             contested when it is already lost.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ChargebacksController.ResolveAsync(System.Guid,YDot.PAY.Application.Features.Refunds.DTOs.ResolveChargebackRequest,System.Threading.CancellationToken)">
             <summary>
             Records the bank's decision, or concedes without contesting.
            
             A LOST CHARGEBACK MOVES THE DONATION TO ChargedBack and takes it out of the receiptable
             set - the money is gone, and a tax receipt for it would let the donor claim relief on a
             gift the charity never kept.
             </summary>
        </member>
        <member name="T:YDot.PAY.Api.Controllers.DonationIntentsController">
             <summary>
             The staff view of donation intents - SCR-PAY-001 - and the payment support queue, section 23.
            
             THE SAME RECORDS AS <see cref="T:YDot.PAY.Api.Controllers.PublicDonationsController"/>, THROUGH A DIFFERENT DOOR. There
             the caller is a donor holding one unguessable reference; here the caller holds a token, a
             permission and an Organisation, and can see every intent that Organisation owns. Keeping the
             two apart means an endpoint's authorization is a fact about the endpoint rather than the
             result of a branch inside it.
            
             <c>ActiveUserOnly</c> ON THE CLASS blocks a suspended account that still holds a live token -
             a permission check alone would not, because the claims in the token were true when it was
             issued.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.DonationIntentsController.#ctor(YDot.PAY.Application.Features.Donations.Commands.ManageIntent.DonationIntentCommandHandler,YDot.PAY.Application.Features.Donations.Queries.DonationQueryHandler)">
             <summary>
             The staff view of donation intents - SCR-PAY-001 - and the payment support queue, section 23.
            
             THE SAME RECORDS AS <see cref="T:YDot.PAY.Api.Controllers.PublicDonationsController"/>, THROUGH A DIFFERENT DOOR. There
             the caller is a donor holding one unguessable reference; here the caller holds a token, a
             permission and an Organisation, and can see every intent that Organisation owns. Keeping the
             two apart means an endpoint's authorization is a fact about the endpoint rather than the
             result of a branch inside it.
            
             <c>ActiveUserOnly</c> ON THE CLASS blocks a suspended account that still holds a live token -
             a permission check alone would not, because the claims in the token were true when it was
             issued.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.DonationIntentsController.SearchAsync(YDot.PAY.Application.Features.Donations.DTOs.DonationIntentSearchFilter,System.Threading.CancellationToken)">
            <summary>The intent register.</summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.DonationIntentsController.GetDonationIntentAsync(System.Guid,System.Threading.CancellationToken)">
             <summary>
             One intent in full, with its whole attempt history - section 24.
            
             VIEWING UNMASKED DONOR DETAIL IS AUDITED by the handler. Holding the permission is not the
             same as using it, and "who looked at this donor's tax identifier?" is a question a
             data-protection review asks.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.DonationIntentsController.ResendLinkAsync(System.Guid,YDot.PAY.Api.Controllers.ResendPaymentLinkBody,System.Threading.CancellationToken)">
             <summary>
             Sends the payment link again.
            
             The commonest support action there is: a donor who lost the e-mail, or whose link expired
             before they got to it. It reuses the intent rather than creating a second one, so the
             donor cannot end up holding two live links for one gift.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.DonationIntentsController.CancelAsync(System.Guid,YDot.PAY.Application.Features.Donations.DTOs.CancelDonationIntentRequest,System.Threading.CancellationToken)">
             <summary>
             Cancels an intent.
            
             STAFF ONLY, and never available once the intent is paid. Cancelling a paid intent would
             leave a donation attached to a cancelled intention, which is a contradiction the reports
             cannot express.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.DonationIntentsController.GetSupportQueueAsync(YDot.PAY.Application.Common.Models.PaginationRequest,System.Threading.CancellationToken)">
             <summary>
             Section 23: the payment support queue.
            
             Intents that failed and need a person - which is narrower than "failed": one that failed
             once and was then paid needs nobody. What lands here has either exhausted the retry
             allowance or has an attempt whose outcome is UNKNOWN, the second being the more urgent
             because unknown means the donor may already have been charged.
             </summary>
        </member>
        <member name="T:YDot.PAY.Api.Controllers.ResendPaymentLinkBody">
             <summary>
             The body of a resend request.
            
             IT EXISTS ONLY TO CARRY THE EXPECTED VERSION, which is why it is a one-property type rather
             than a query parameter: optimistic concurrency belongs in the body with the rest of the
             request, and a version in a query string is one a browser will happily cache and replay.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ResendPaymentLinkBody.#ctor(System.Int64)">
             <summary>
             The body of a resend request.
            
             IT EXISTS ONLY TO CARRY THE EXPECTED VERSION, which is why it is a one-property type rather
             than a query parameter: optimistic concurrency belongs in the body with the rest of the
             request, and a version in a query string is one a browser will happily cache and replay.
             </summary>
        </member>
        <member name="T:YDot.PAY.Api.Controllers.DonationsController">
             <summary>
             The donation register - money that actually arrived.
            
             THE ACTIONS THAT HANG OFF A DONATION LIVE HERE rather than on the receipt and refund
             controllers, because they are things done TO a donation: issue its receipt, raise a refund
             against it. The resulting record is then managed on its own controller. That split follows how
             the screens work - an operator opens a donation and issues its receipt; they open the receipt
             register to correct one.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.DonationsController.#ctor(YDot.PAY.Application.Features.Donations.Queries.DonationQueryHandler,YDot.PAY.Application.Features.Donations.Commands.ManageDonation.DonationCommandHandler,YDot.PAY.Application.Features.Receipts.Commands.ManageReceipt.ReceiptCommandHandler,YDot.PAY.Application.Features.Refunds.Commands.ManageRefund.RefundCommandHandler)">
             <summary>
             The donation register - money that actually arrived.
            
             THE ACTIONS THAT HANG OFF A DONATION LIVE HERE rather than on the receipt and refund
             controllers, because they are things done TO a donation: issue its receipt, raise a refund
             against it. The resulting record is then managed on its own controller. That split follows how
             the screens work - an operator opens a donation and issues its receipt; they open the receipt
             register to correct one.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.DonationsController.SearchAsync(YDot.PAY.Application.Features.Donations.DTOs.DonationSearchFilter,System.Threading.CancellationToken)">
            <summary>The donation register.</summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.DonationsController.GetStatisticsAsync(System.Threading.CancellationToken)">
             <summary>
             Counts and totals for the register's summary tiles.
            
             The totals are reported in the Organisation's PREDOMINANT currency rather than summed
             across currencies - adding a rupee to a dollar is the one arithmetic a money total must
             never do, and a tile made of three currencies looks authoritative and means nothing.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.DonationsController.ExportAsync(YDot.PAY.Application.Features.Donations.DTOs.DonationSearchFilter,System.Threading.CancellationToken)">
             <summary>
             The CSV export.
            
             THE DONOR COLUMNS ARE MASKED IN THE FILE unless the caller holds the sensitive-donor
             permission - if anything a CSV needs the masking more than a screen does, because it
             outlives the session that produced it and travels by e-mail.
            
             The response carries an X-Export-Reference header tying the file back to the audit row
             recording who exported it.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.DonationsController.GetDonationAsync(System.Guid,System.Threading.CancellationToken)">
            <summary>One donation in full, with its receipts, refunds and chargebacks.</summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.DonationsController.RecordOfflineAsync(YDot.PAY.Application.Features.Donations.DTOs.RecordOfflineDonationRequest,System.Threading.CancellationToken)">
             <summary>
             Records a gift taken outside the gateway: a cheque, a bank transfer, cash at an event.
            
             IT STILL GOES THROUGH AN INTENT, so an offline gift carries the same attribution and
             consent record as a card payment and appears in exactly the same reports.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.DonationsController.ReconcileAsync(System.Guid,YDot.PAY.Application.Features.Donations.DTOs.ReconcileDonationRequest,System.Threading.CancellationToken)">
             <summary>
             Marks a donation reconciled against a bank statement.
            
             AN ASSERTION BY A PERSON, audited with their identity: "matched" means somebody looked at
             a statement line and this donation and said they are the same money.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.DonationsController.IssueReceiptAsync(System.Guid,YDot.PAY.Application.Features.Receipts.DTOs.IssueReceiptRequest,System.Threading.CancellationToken)">
             <summary>
             Issues the tax receipt for a donation - section 24.
            
             THE AMOUNT IS NOT A PARAMETER. A receipt is for what was actually given, less anything
             refunded, and letting a caller choose the figure on a tax document is exactly the hole a
             receipt exists to close.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.DonationsController.RequestRefundAsync(System.Guid,YDot.PAY.Application.Features.Refunds.DTOs.RequestRefundRequest,System.Threading.CancellationToken)">
             <summary>
             Raises a refund against a donation.
            
             RAISING IS NOT APPROVING. This opens a case; a DIFFERENT person has to approve it before
             any money moves, which the handler enforces per record rather than by permission.
             </summary>
        </member>
        <member name="T:YDot.PAY.Api.Controllers.GatewayAccountsController">
             <summary>
             Each organisation's payment gateway configuration.
            
             THIS IS WHERE THE MODULE'S TENANCY STOPS BEING A DATA QUESTION AND BECOMES A LEGAL ONE. Every
             charity collects into its OWN merchant account; a shared one would pool several organisations'
             income into a single settlement, which no amount of correct reporting afterwards would fix.
            
             NO SECRET PASSES THROUGH THESE ENDPOINTS, in either direction. The request carries a
             REFERENCE to a key already placed in the secret store, and the response says only whether a
             key is configured. A merchant secret in a request body ends up in a request log, a proxy
             buffer and an exception message; in a response it ends up in the browser's memory and its dev
             tools.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.GatewayAccountsController.#ctor(YDot.PAY.Application.Features.Gateway.Commands.ManageGatewayAccount.GatewayAccountCommandHandler)">
             <summary>
             Each organisation's payment gateway configuration.
            
             THIS IS WHERE THE MODULE'S TENANCY STOPS BEING A DATA QUESTION AND BECOMES A LEGAL ONE. Every
             charity collects into its OWN merchant account; a shared one would pool several organisations'
             income into a single settlement, which no amount of correct reporting afterwards would fix.
            
             NO SECRET PASSES THROUGH THESE ENDPOINTS, in either direction. The request carries a
             REFERENCE to a key already placed in the secret store, and the response says only whether a
             key is configured. A merchant secret in a request body ends up in a request log, a proxy
             buffer and an exception message; in a response it ends up in the browser's memory and its dev
             tools.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.GatewayAccountsController.GetAllAsync(System.Threading.CancellationToken)">
             <summary>
             Every account this organisation holds, live and test alike.
            
             BOTH ARE SHOWN, with the test flag prominent on each. Hiding test accounts would leave an
             operator unable to see why donations are not reaching their bank; showing them
             indistinguishably is how an organisation ends up reporting income it never received.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.GatewayAccountsController.UpsertAsync(YDot.PAY.Application.Features.Gateway.DTOs.UpsertGatewayAccountRequest,System.Threading.CancellationToken)">
             <summary>
             Creates or updates the account for a gateway and mode.
            
             AN UPSERT RATHER THAN SEPARATE POST AND PUT, because the natural key is (organisation,
             gateway, test mode) rather than an id the caller holds. A configuration screen that had to
             know whether a row already existed would have to ask first, and the answer could change
             between the two calls.
            
             <c>ExpectedVersion</c> is optional precisely because of that: absent means "create", and
             present means "update this version of it".
             </summary>
        </member>
        <member name="T:YDot.PAY.Api.Controllers.PaymentsController">
             <summary>
             Payment verification, the gateway event queue and safe retry - SCR-PAY-002, SCR-PAY-003 and
             SCR-PAY-007.
            
             THIS IS THE OPERATIONAL HEART OF THE MODULE. Everything here exists because payments do not
             always resolve cleanly: a webhook that arrived twice, an attempt that timed out, a donor who
             says they paid and a register that says they did not.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.PaymentsController.#ctor(YDot.PAY.Application.Features.Payments.Commands.ProcessPayment.PaymentProcessingCommandHandler,YDot.PAY.Application.Features.Payments.Queries.PaymentEventQueryHandler)">
             <summary>
             Payment verification, the gateway event queue and safe retry - SCR-PAY-002, SCR-PAY-003 and
             SCR-PAY-007.
            
             THIS IS THE OPERATIONAL HEART OF THE MODULE. Everything here exists because payments do not
             always resolve cleanly: a webhook that arrived twice, an attempt that timed out, a donor who
             says they paid and a register that says they did not.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.PaymentsController.VerifyAsync(YDot.PAY.Application.Features.Payments.DTOs.VerifyPaymentRequest,System.Threading.CancellationToken)">
             <summary>
             Asks the gateway what actually happened to an attempt.
            
             THE STAFF VERSION, taking an attempt id. The donor's version takes an intent reference and
             lives on the public controller; both reach the same handler and neither ever retries the
             payment, because a retry disguised as a check is how somebody gets charged twice.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.PaymentsController.SafeRetryAsync(System.Guid,YDot.PAY.Application.Features.Payments.DTOs.SafeRetryRequest,System.Threading.CancellationToken)">
             <summary>
             Safe retry - section 23.
            
             NOT A PLAIN RETRY. The handler verifies the previous attempt with the gateway FIRST and
             refuses if it actually succeeded, which is the whole difference between helping a donor
             whose card was declined and charging one who has already paid. The outcome says which of
             the four things happened - Retried, AlreadyPaid, StillPending or Refused - so the operator
             can tell the donor something true.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.PaymentsController.SearchEventsAsync(YDot.PAY.Application.Features.Payments.DTOs.PaymentEventSearchFilter,System.Threading.CancellationToken)">
            <summary>The event queue.</summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.PaymentsController.GetEventAsync(System.Guid,System.Threading.CancellationToken)">
             <summary>
             One queued event, with its verbatim payload.
            
             THE RAW PAYLOAD IS WITHHELD unless the caller holds the events permission - a gateway body
             can contain donor contact details that the masked views deliberately hide, and it has no
             fixed shape to mask reliably.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.PaymentsController.ReprocessEventAsync(System.Guid,YDot.PAY.Application.Features.Payments.DTOs.ReprocessPaymentEventRequest,System.Threading.CancellationToken)">
             <summary>
             Re-runs a failed event through the processor.
            
             IT REFUSES AN EVENT THAT ALREADY APPLIED. Reprocessing one of those would record the
             donation a second time, which is the exact failure the queue exists to prevent.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.PaymentsController.DismissEventAsync(System.Guid,YDot.PAY.Application.Features.Payments.DTOs.DismissPaymentEventRequest,System.Threading.CancellationToken)">
             <summary>
             Marks an event as needing no action.
            
             THE EVENT IS KEPT, NOT DELETED. Dismissing says "a person looked at this and decided
             nothing was needed", which is a more useful statement than the row not existing - and it
             records who decided.
             </summary>
        </member>
        <member name="T:YDot.PAY.Api.Controllers.PublicDonationsController">
             <summary>
             The donor-facing donation flow - sections 11 to 14 and 19 to 23.
            
             EVERY ACTION HERE IS <c>[AllowAnonymous]</c>, and that is the defining fact about this
             controller. The donor is a stranger with a QR code, a link in an e-mail or a button on a web
             page; they have no account, no token and no session. Requiring one would mean asking somebody
             to register before they are allowed to give money, which is the single most reliable way to
             lose a donation.
            
             SO WHAT PROTECTS IT? Four things, and it is worth being explicit because "anonymous" and
             "unprotected" are not the same word:
            
               1. THE ORGANISATION IS NEVER TAKEN FROM THE BODY. It is resolved by the tenant middleware
                  from the intent reference in the route, or from the tracking reference the donor
                  followed. A caller cannot name a charity to donate against.
               2. EVERY REFERENCE IS UNGUESSABLE - twelve characters from a 31-character alphabet, about
                  59 bits. Reaching somebody else's donation means guessing one, not incrementing one.
               3. NOTHING HERE RETURNS UNMASKED DONOR DETAIL. The result page shows a masked e-mail even
                  to the donor themselves, because the endpoint is reachable by anybody holding the link.
               4. THE PERMITTED ACTIONS COME BACK EMPTY OF STAFF ACTIONS, because they are computed with a
                  permission probe that answers false to everything.
            
             THE STAFF EQUIVALENTS OF THESE ROUTES LIVE IN <see cref="T:YDot.PAY.Api.Controllers.DonationIntentsController"/> and
             require both a token and a permission. Two controllers rather than one set of endpoints with
             a conditional: an endpoint that is anonymous under some conditions is one whose security is
             decided by a branch somebody will eventually get wrong.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.PublicDonationsController.#ctor(YDot.PAY.Application.Features.Donations.Commands.ManageIntent.DonationIntentCommandHandler,YDot.PAY.Application.Features.Donations.Queries.DonationQueryHandler,YDot.PAY.Application.Features.Payments.Commands.ProcessPayment.PaymentProcessingCommandHandler)">
             <summary>
             The donor-facing donation flow - sections 11 to 14 and 19 to 23.
            
             EVERY ACTION HERE IS <c>[AllowAnonymous]</c>, and that is the defining fact about this
             controller. The donor is a stranger with a QR code, a link in an e-mail or a button on a web
             page; they have no account, no token and no session. Requiring one would mean asking somebody
             to register before they are allowed to give money, which is the single most reliable way to
             lose a donation.
            
             SO WHAT PROTECTS IT? Four things, and it is worth being explicit because "anonymous" and
             "unprotected" are not the same word:
            
               1. THE ORGANISATION IS NEVER TAKEN FROM THE BODY. It is resolved by the tenant middleware
                  from the intent reference in the route, or from the tracking reference the donor
                  followed. A caller cannot name a charity to donate against.
               2. EVERY REFERENCE IS UNGUESSABLE - twelve characters from a 31-character alphabet, about
                  59 bits. Reaching somebody else's donation means guessing one, not incrementing one.
               3. NOTHING HERE RETURNS UNMASKED DONOR DETAIL. The result page shows a masked e-mail even
                  to the donor themselves, because the endpoint is reachable by anybody holding the link.
               4. THE PERMITTED ACTIONS COME BACK EMPTY OF STAFF ACTIONS, because they are computed with a
                  permission probe that answers false to everything.
            
             THE STAFF EQUIVALENTS OF THESE ROUTES LIVE IN <see cref="T:YDot.PAY.Api.Controllers.DonationIntentsController"/> and
             require both a token and a permission. Two controllers rather than one set of endpoints with
             a conditional: an endpoint that is anonymous under some conditions is one whose security is
             decided by a branch somebody will eventually get wrong.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.PublicDonationsController.InitiateAsync(YDot.PAY.Application.Features.Donations.DTOs.CreateDonationIntentRequest,System.Threading.CancellationToken)">
             <summary>
             Starts a donation. Section 11 and section 22's nine entry channels.
            
             ONE ENDPOINT FOR EVERY CHANNEL. A QR scan, a website button, an e-mail link and a
             fundraiser's lead link differ only in their attribution - the SourceType, the tracking
             reference and the lead - so they differ only in the values on this request, never in the
             shape of it or in the decision that follows.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.PublicDonationsController.CheckDonorAsync(System.String,System.Threading.CancellationToken)">
             <summary>
             Section 12: is this donor already known to this charity?
            
             A BRANCH, NOT AN ERROR. A match sends the donor to sign in with the intent preserved
             (section 13); no match lets them continue straight to payment without creating a password
             first (section 14).
            
             IT TAKES THE INTENT REFERENCE RATHER THAN AN E-MAIL, deliberately. An endpoint that
             accepted a bare address would be an oracle: type an address, learn whether that person
             gives to this charity. Working from a reference the caller already holds means they can
             only ask about a donation they started.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.PublicDonationsController.CreatePaymentLinkAsync(System.String,YDot.PAY.Application.Features.Donations.DTOs.CreatePaymentLinkRequest,System.Threading.CancellationToken)">
             <summary>
             Issues the payment link and opens an attempt.
            
             THE EXPECTED VERSION IS ON THE BODY because two tabs, or a double-clicked button, would
             otherwise open two attempts against one intent - two links, and a donor who pays both has
             given twice.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.PublicDonationsController.CreateCheckoutSessionAsync(System.String,YDot.PAY.Application.Features.Donations.DTOs.CreateCheckoutSessionRequest,System.Threading.CancellationToken)">
             <summary>
             Opens the provider's checkout for this donation, so the donor pays without leaving us.
            
             THIS IS THE ROUTE SUBMIT TAKES. The donor pressed a button on a form they are looking at;
             the payment form should open in front of them. The payment-link endpoint above stays for
             what a link is genuinely for - reaching somebody who is NOT at a screen - and is also what
             the client falls back to when the organisation's provider cannot draw an in-page checkout.
            
             WHAT COMES BACK CARRIES NO SECRET. An order id, the merchant's publishable key, the amount
             for display and the donor's own details to prefill. The price is the order's, held by the
             provider, so a browser that edits this response changes a label and nothing else.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.PublicDonationsController.ConfirmCheckoutAsync(System.String,YDot.PAY.Application.Features.Donations.DTOs.ConfirmCheckoutRequest,System.Threading.CancellationToken)">
             <summary>
             Takes the signed result of a finished checkout and settles the donation.
            
             ANONYMOUS, LIKE EVERYTHING ELSE HERE, AND SAFE FOR THE SAME REASON THE OTHERS ARE - with
             one addition that matters more here than anywhere. The body is a claim made by a browser
             that a payment happened, which is exactly the claim an attacker would like to make. It is
             believed only because it carries a signature made with the merchant secret, checked on the
             server before anything is written. Without that this endpoint would let anybody mark any
             donation paid by naming its reference.
            
             IT DOES NOT DECIDE THE OUTCOME EITHER. A good signature earns the payment id the right to
             be recorded; whether money actually moved is then asked of the provider directly, through
             the same verification the result page and Support and Retry use.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.PublicDonationsController.GetAsync(System.String,System.Threading.CancellationToken)">
             <summary>
             The donor's own view of their donation, for the result page they land on after paying.
            
             ALWAYS MASKED, even though it is the donor's own record. There is no session to prove who
             is holding the link, so the safe branch is the only branch.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.PublicDonationsController.VerifyAsync(System.String,System.Threading.CancellationToken)">
             <summary>
             Asks the gateway what actually happened - SCR-PAY-002.
            
             THE DONOR CAN CALL THIS THEMSELVES, which is the point of the verification screen: a donor
             whose browser was closed mid-payment, or who saw a spinner and nothing else, needs a way to
             find out whether their money left their account. Without it the only recourse is a support
             call, and the support agent asks the gateway the same question.
            
             IT NEVER RETRIES. Verification asks; it does not pay. A retry disguised as a check is how a
             donor gets charged twice.
             </summary>
        </member>
        <member name="T:YDot.PAY.Api.Controllers.ReceiptsController">
             <summary>
             The receipt register - SCR-PAY-005 - and the corrections that keep it honest.
            
             THERE IS NO PUT AND NO DELETE ON THIS CONTROLLER, and their absence is the design. A receipt
             is a tax document: a donor may have claimed relief on the version they hold, and an auditor
             may ask to see it years later. So it is never edited and never removed - it is CORRECTED,
             which issues a NEW version pointing back at what it supersedes, or VOIDED, which leaves the
             row in place marked void with a reason.
            
             A VOID DOES NOT FREE ITS NUMBER either. Receipt numbers run in an unbroken per-organisation
             series, and a gap is exactly what a tax authority reads as a destroyed receipt.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ReceiptsController.#ctor(YDot.PAY.Application.Features.Receipts.Queries.ReceiptQueryHandler,YDot.PAY.Application.Features.Receipts.Commands.ManageReceipt.ReceiptCommandHandler)">
             <summary>
             The receipt register - SCR-PAY-005 - and the corrections that keep it honest.
            
             THERE IS NO PUT AND NO DELETE ON THIS CONTROLLER, and their absence is the design. A receipt
             is a tax document: a donor may have claimed relief on the version they hold, and an auditor
             may ask to see it years later. So it is never edited and never removed - it is CORRECTED,
             which issues a NEW version pointing back at what it supersedes, or VOIDED, which leaves the
             row in place marked void with a reason.
            
             A VOID DOES NOT FREE ITS NUMBER either. Receipt numbers run in an unbroken per-organisation
             series, and a gap is exactly what a tax authority reads as a destroyed receipt.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ReceiptsController.SearchAsync(YDot.PAY.Application.Features.Receipts.DTOs.ReceiptSearchFilter,System.Threading.CancellationToken)">
            <summary>The receipt register.</summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ReceiptsController.GetRegisterAsync(YDot.PAY.Application.Features.Receipts.DTOs.ReceiptRegisterFilter,System.Threading.CancellationToken)">
             <summary>
             The Receipt Register - rows and the four totals, in one call.
            
             SEPARATE FROM THE PLAIN LIST ABOVE. This one includes failed payments, which have no
             receipt, because the workflow document's register shows the outcome of every payment
             rather than the set of tax documents that exist.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ReceiptsController.ExportAsync(YDot.PAY.Application.Features.Receipts.DTOs.ReceiptSearchFilter,System.Threading.CancellationToken)">
            <summary>
            The CSV export - the file a finance team files with the year's return.
            </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ReceiptsController.GetReceiptAsync(System.Guid,System.Threading.CancellationToken)">
             <summary>
             One receipt in full, with its delivery history.
            
             The DONOR DETAILS ARE THE SNAPSHOT AS ISSUED, not the donor's details today. A receipt is
             a statement about a moment, and one that silently updated itself would disagree with the
             document in the donor's hand.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ReceiptsController.CorrectAsync(System.Guid,YDot.PAY.Application.Features.Receipts.DTOs.CorrectReceiptRequest,System.Threading.CancellationToken)">
             <summary>
             Corrects an issued receipt.
            
             A CORRECTION IS A NEW VERSION, never an edit, and it takes the NEXT number in the series
             rather than reusing the original's. The original stays exactly as issued, because a donor
             who claimed relief on version 1 must still be able to show what version 1 said.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ReceiptsController.VoidAsync(System.Guid,YDot.PAY.Application.Features.Receipts.DTOs.VoidReceiptRequest,System.Threading.CancellationToken)">
             <summary>
             Voids a receipt outright, where a correction is not enough.
            
             A REASON IS MANDATORY, and the database enforces it as well as the handler. "Void, reason
             unknown" is precisely the record an auditor challenges.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.ReceiptsController.ResendAsync(System.Guid,YDot.PAY.Application.Features.Receipts.DTOs.ResendReceiptRequest,System.Threading.CancellationToken)">
             <summary>
             Sends an issued receipt again.
            
             SENDING TO A DIFFERENT ADDRESS IS AUDITED, because posting somebody's tax document
             somewhere other than the address on it is exactly the action that needs justifying later.
             </summary>
        </member>
        <member name="T:YDot.PAY.Api.Controllers.RefundsController">
             <summary>
             The refund register - SCR-PAY-006.
            
             RAISING A REFUND LIVES ON THE DONATION CONTROLLER, because that is where an operator is
             standing when they decide one is needed. Everything after that - approving, rejecting,
             tracking - lives here, on the case.
            
             THE SEGREGATION OF DUTIES IS THE POINT OF THIS CONTROLLER. Approve and reject are refused to
             the person who raised the case, WHATEVER PERMISSIONS THEY HOLD. That is a rule about who,
             relative to this record - which no permission code can express - so it is enforced in the
             handler and reflected in the permitted-action list, meaning the screen never draws a button
             that will answer 409.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.RefundsController.#ctor(YDot.PAY.Application.Features.Refunds.Queries.RefundQueryHandler,YDot.PAY.Application.Features.Refunds.Commands.ManageRefund.RefundCommandHandler)">
             <summary>
             The refund register - SCR-PAY-006.
            
             RAISING A REFUND LIVES ON THE DONATION CONTROLLER, because that is where an operator is
             standing when they decide one is needed. Everything after that - approving, rejecting,
             tracking - lives here, on the case.
            
             THE SEGREGATION OF DUTIES IS THE POINT OF THIS CONTROLLER. Approve and reject are refused to
             the person who raised the case, WHATEVER PERMISSIONS THEY HOLD. That is a rule about who,
             relative to this record - which no permission code can express - so it is enforced in the
             handler and reflected in the permitted-action list, meaning the screen never draws a button
             that will answer 409.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.RefundsController.SearchAsync(YDot.PAY.Application.Features.Refunds.DTOs.RefundSearchFilter,System.Threading.CancellationToken)">
            <summary>The refund register.</summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.RefundsController.ExportAsync(YDot.PAY.Application.Features.Refunds.DTOs.RefundSearchFilter,System.Threading.CancellationToken)">
            <summary>The CSV export.</summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.RefundsController.GetRefundAsync(System.Guid,System.Threading.CancellationToken)">
             <summary>
             One refund case in full.
            
             The permitted-action list on the response is computed for THIS caller: somebody looking at
             a case they raised themselves gets View and Export, and no Approve.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.RefundsController.ApproveAsync(System.Guid,YDot.PAY.Application.Features.Refunds.DTOs.DecideRefundRequest,System.Threading.CancellationToken)">
             <summary>
             Approves a refund, which is what actually sends money back.
            
             REFUSED TO THE PERSON WHO RAISED IT. Money leaving the organisation needs two people, and
             this is where that is enforced.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.RefundsController.RejectAsync(System.Guid,YDot.PAY.Application.Features.Refunds.DTOs.RejectRefundRequest,System.Threading.CancellationToken)">
             <summary>
             Rejects a refund.
            
             A REASON IS MANDATORY. Somebody asked for this and is entitled to an answer, and "rejected"
             with nothing beside it is the kind of record that generates a second identical request a
             week later.
             </summary>
        </member>
        <member name="T:YDot.PAY.Api.Controllers.WebhooksController">
             <summary>
             Where payment providers post their callbacks.
            
             THIS IS THE MOST EXPOSED SURFACE IN THE PLATFORM: a public, unauthenticated POST endpoint that
             can cause a donation to be recorded. Four decisions defend it, and each one is doing real
             work.
            
               1. THE SIGNATURE IS THE ONLY AUTHENTICATION. Anybody can post here. The HMAC over the raw
                  body, checked against the merchant's webhook secret, is what distinguishes the provider
                  from somebody who read our API documentation. An event that fails is STORED and never
                  acted on - keeping it is what makes an attempted forgery visible afterwards.
               2. THE RAW BODY IS READ BEFORE MODEL BINDING. The signature is computed over the exact
                  bytes the provider sent; a body that has been through a JSON deserialiser and back has
                  different whitespace and different key order, and would never verify. That is why this
                  controller reads the stream itself instead of taking a typed parameter.
               3. IT ALWAYS ANSWERS 200. A non-2xx makes providers retry for days and eventually disable
                  the endpoint, and there is nothing a provider can usefully do about our processing
                  failure anyway. The event is queued; a person works the queue. The one thing that must
                  never happen is a provider giving up on a genuine capture because we answered 500.
               4. NOTHING IS PROCESSED INLINE. The handler stores the event and returns. Applying it -
                  recording the donation, issuing the receipt - happens against the stored row, so a
                  redelivery of the same event id is recognised as the duplicate it is.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.WebhooksController.#ctor(YDot.PAY.Application.Features.Payments.Commands.ProcessPayment.PaymentProcessingCommandHandler,Microsoft.Extensions.Logging.ILogger{YDot.PAY.Api.Controllers.WebhooksController})">
             <summary>
             Where payment providers post their callbacks.
            
             THIS IS THE MOST EXPOSED SURFACE IN THE PLATFORM: a public, unauthenticated POST endpoint that
             can cause a donation to be recorded. Four decisions defend it, and each one is doing real
             work.
            
               1. THE SIGNATURE IS THE ONLY AUTHENTICATION. Anybody can post here. The HMAC over the raw
                  body, checked against the merchant's webhook secret, is what distinguishes the provider
                  from somebody who read our API documentation. An event that fails is STORED and never
                  acted on - keeping it is what makes an attempted forgery visible afterwards.
               2. THE RAW BODY IS READ BEFORE MODEL BINDING. The signature is computed over the exact
                  bytes the provider sent; a body that has been through a JSON deserialiser and back has
                  different whitespace and different key order, and would never verify. That is why this
                  controller reads the stream itself instead of taking a typed parameter.
               3. IT ALWAYS ANSWERS 200. A non-2xx makes providers retry for days and eventually disable
                  the endpoint, and there is nothing a provider can usefully do about our processing
                  failure anyway. The event is queued; a person works the queue. The one thing that must
                  never happen is a provider giving up on a genuine capture because we answered 500.
               4. NOTHING IS PROCESSED INLINE. The handler stores the event and returns. Applying it -
                  recording the donation, issuing the receipt - happens against the stored row, so a
                  redelivery of the same event id is recognised as the duplicate it is.
             </summary>
        </member>
        <member name="F:YDot.PAY.Api.Controllers.WebhooksController.SignatureHeaderNames">
             <summary>
             The headers different providers use for their signature.
            
             A LIST RATHER THAN ONE NAME because every provider chose differently, and the alternative
             is a separate endpoint per provider that is identical apart from one string.
             </summary>
        </member>
        <member name="F:YDot.PAY.Api.Controllers.WebhooksController.MaximumPayloadBytes">
            <summary>The largest body accepted, in bytes.</summary>
        </member>
        <member name="M:YDot.PAY.Api.Controllers.WebhooksController.ReceiveAsync(System.String,System.Threading.CancellationToken)">
             <summary>
             Receives one provider callback.
            
             <paramref name="gatewayName"/> IS IN THE ROUTE, not the body, so the signature is checked
             against the right secret before anything in the body is trusted. Reading the provider name
             out of an unverified payload would let a forger choose which secret we check against.
             </summary>
        </member>
        <member name="T:YDot.PAY.Api.Filters.FluentValidationFilter">
             <summary>
             Runs the FluentValidation validator for every action argument that has one, before the
             action executes.
            
             WHY A FILTER RATHER THAN A CALL IN EACH HANDLER. A validator invoked by the handler is one a
             handler can forget to invoke, and the endpoint that forgets is the one nobody reviews. A
             filter applies to every action by construction, so a request body reaching a handler has
             always been validated.
            
             THE VALIDATOR IS RESOLVED BY THE ARGUMENT'S TYPE, so an action with no validator for its
             body simply passes through - which is the right behaviour for the lifecycle endpoints whose
             only real rule lives in the handler.
            
             IT WRITES THE SAME ENVELOPE as everything else, so a validation failure and a business
             refusal are the same shape to the client. Without that the Angular error handler would need
             a second code path for exactly one kind of failure.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Filters.FluentValidationFilter.#ctor(System.IServiceProvider)">
             <summary>
             Runs the FluentValidation validator for every action argument that has one, before the
             action executes.
            
             WHY A FILTER RATHER THAN A CALL IN EACH HANDLER. A validator invoked by the handler is one a
             handler can forget to invoke, and the endpoint that forgets is the one nobody reviews. A
             filter applies to every action by construction, so a request body reaching a handler has
             always been validated.
            
             THE VALIDATOR IS RESOLVED BY THE ARGUMENT'S TYPE, so an action with no validator for its
             body simply passes through - which is the right behaviour for the lifecycle endpoints whose
             only real rule lives in the handler.
            
             IT WRITES THE SAME ENVELOPE as everything else, so a validation failure and a business
             refusal are the same shape to the client. Without that the Angular error handler would need
             a second code path for exactly one kind of failure.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Filters.FluentValidationFilter.ToCamelCase(System.String)">
             <summary>
             "OwnerIds" becomes "ownerIds"; "Places[0].PlaceName" becomes "places[0].placeName".
            
             The nested case matters: FluentValidation reports a child collection failure with the
             full path, and a form binding to that path needs every segment camel-cased, not just the
             first.
             </summary>
        </member>
        <member name="T:YDot.PAY.Api.Middleware.CorrelationIdMiddleware">
             <summary>
             Gives every request one identifier that ties the HTTP call, the log lines and the audit rows
             together.
            
             IT RUNS FIRST IN THE PIPELINE, so even a request rejected at the very edge - a malformed
             token, a failed authorization - still carries an id somebody can quote. An id assigned later
             would be missing from exactly the responses people ask about.
            
             AN INBOUND HEADER IS HONOURED, so a correlation id set by the Angular client or by a gateway
             survives into this service's logs and the trail can be followed across the whole hop. It is
             LENGTH-CAPPED, because it ends up in a bounded audit column and in log lines - an unbounded
             caller-supplied string in both places is a log-flooding vector.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Middleware.CorrelationIdMiddleware.#ctor(Microsoft.AspNetCore.Http.RequestDelegate)">
             <summary>
             Gives every request one identifier that ties the HTTP call, the log lines and the audit rows
             together.
            
             IT RUNS FIRST IN THE PIPELINE, so even a request rejected at the very edge - a malformed
             token, a failed authorization - still carries an id somebody can quote. An id assigned later
             would be missing from exactly the responses people ask about.
            
             AN INBOUND HEADER IS HONOURED, so a correlation id set by the Angular client or by a gateway
             survives into this service's logs and the trail can be followed across the whole hop. It is
             LENGTH-CAPPED, because it ends up in a bounded audit column and in log lines - an unbounded
             caller-supplied string in both places is a log-flooding vector.
             </summary>
        </member>
        <member name="T:YDot.PAY.Api.Middleware.ExceptionHandlingMiddleware">
             <summary>
             The last line of defence: turns an unhandled exception into the same six-key envelope every
             endpoint returns.
            
             WITHOUT THIS, an unhandled exception produces either an empty 500 or - in development - a
             full stack trace as HTML, and the Angular interceptor that expects the envelope on every
             response gets neither <c>message</c> nor <c>errorCode</c> to read.
            
             THE EXCEPTION DETAIL NEVER REACHES THE CLIENT. It is logged with the correlation id, and the
             caller is given that id and a generic message. A stack trace tells an attacker which ORM,
             which version and which table names are in play, and tells a legitimate user nothing they
             can act on.
            
             WHAT IT REPLACES. The old middleware caught <c>CustomValidationException</c> and turned it
             into a 400. That exception is gone: validation is FluentValidation running in the pipeline
             and a refusal is a Result, so a validation failure never unwinds through here any more.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Middleware.ExceptionHandlingMiddleware.#ctor(Microsoft.AspNetCore.Http.RequestDelegate,Microsoft.Extensions.Logging.ILogger{YDot.PAY.Api.Middleware.ExceptionHandlingMiddleware},Microsoft.Extensions.Hosting.IHostEnvironment)">
             <summary>
             The last line of defence: turns an unhandled exception into the same six-key envelope every
             endpoint returns.
            
             WITHOUT THIS, an unhandled exception produces either an empty 500 or - in development - a
             full stack trace as HTML, and the Angular interceptor that expects the envelope on every
             response gets neither <c>message</c> nor <c>errorCode</c> to read.
            
             THE EXCEPTION DETAIL NEVER REACHES THE CLIENT. It is logged with the correlation id, and the
             caller is given that id and a generic message. A stack trace tells an attacker which ORM,
             which version and which table names are in play, and tells a legitimate user nothing they
             can act on.
            
             WHAT IT REPLACES. The old middleware caught <c>CustomValidationException</c> and turned it
             into a 400. That exception is gone: validation is FluentValidation running in the pipeline
             and a refusal is a Result, so a validation failure never unwinds through here any more.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.Middleware.ExceptionHandlingMiddleware.Describe(System.Exception)">
             <summary>
             Maps an exception to a catalogue error.
            
             The concurrency case is the one worth handling explicitly. EF throws
             <c>DbUpdateConcurrencyException</c> when the version column moved between read and write
             - which is the SAME condition the handlers check for and report as a 409 - so letting it
             fall through to a 500 would give two different answers for one situation depending on
             which of the two detected it first.
             </summary>
        </member>
        <member name="T:YDot.PAY.Api.ServiceContainer.DependencyInjection">
             <summary>
             Registers everything the API layer owns: controllers and their JSON shape, JWT validation,
             the authorization policies, CORS and Swagger.
            
             SWAGGER REPLACES SCALAR. The brief asks for Swagger, and there is a practical reason beyond
             that: IAM, DON and CAM all expose Swagger, and four services in one solution documented through
             two different UIs is a small tax paid on every visit.
             </summary>
        </member>
        <member name="F:YDot.PAY.Api.ServiceContainer.DependencyInjection.CorsPolicyName">
            <summary>Named so <c>UseCors</c> and <c>AddCors</c> cannot drift apart.</summary>
        </member>
        <member name="M:YDot.PAY.Api.ServiceContainer.DependencyInjection.ConfigureApiBehavior(Microsoft.Extensions.DependencyInjection.IMvcBuilder)">
             <summary>
             Replaces the framework's automatic 400 with the standard envelope.
            
             WITHOUT THIS, a malformed body - a string where a Guid was expected - produces a
             ValidationProblemDetails, which is a completely different shape from every other response
             and which the brief explicitly rules out for now.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.ServiceContainer.DependencyInjection.WriteEnvelopeAsync(Microsoft.AspNetCore.Http.HttpContext,YDot.PAY.Application.Common.Results.Error)">
            <summary>Writes an error envelope directly, for the two JWT events that bypass MVC.</summary>
        </member>
        <member name="T:YDot.PAY.Api.ServiceContainer.ForwardedHeadersConfiguration">
             <summary>
             Builds the <see cref="T:Microsoft.AspNetCore.Builder.ForwardedHeadersOptions"/> that recover the real caller address from
             behind nginx.
            
             WHY THIS IS A CLASS RATHER THAN AN OBJECT INITIALISER IN Program.cs, and it is not a matter
             of taste. The options used to be written like this:
            
             <code>
             new ForwardedHeadersOptions
             {
                 KnownNetworks = { },   // intended: "trust every proxy"
                 KnownProxies  = { }
             }
             </code>
            
             <c>KnownNetworks = { }</c> inside an object initialiser is a COLLECTION initialiser, not an
             assignment. It calls <c>Add</c> once per element listed, and nothing is listed — so it adds
             nothing and LEAVES THE DEFAULTS IN PLACE. The defaults are IPv6 loopback and
             <c>::ffff:127.0.0.0/104</c>, so the middleware still trusted loopback and only loopback.
             nginx reaches this service from the compose bridge network (172.20.0.0/16), which is not
             loopback, so <c>ForwardedHeadersMiddleware</c> stopped at the first unknown proxy and applied
             nothing at all. The header was set correctly by nginx, forwarded correctly, and then ignored.
            
             The symptom: every row this service writes recorded <c>::ffff:172.20.0.9</c> — the address of
             the nginx CONTAINER — instead of the caller's own. So every audit row names the proxy, and
             "where did this come from" has one answer for every event ever recorded, which is the same as
             having no answer. IAM had the same defect, where it also flattened the per-IP sign-in rate
             limit into a single global bucket.
            
             Clearing the lists has to be done with <c>Clear()</c>, and that is what happens below.
            
             TRUSTING EVERY PROXY IS STILL NOT WHAT WE WANT. X-Forwarded-For is caller-controlled, so an
             empty known-proxy list lets anybody who can open a socket to this service write whatever
             address they like into the audit trail and step around the per-IP limit. In this compose file
             the container's port IS published to the host (6708), so "only nginx can reach it" is not
             true. The trusted set is therefore explicit: loopback plus the private ranges a container
             network uses, overridable per environment with
            
             <code>ForwardedHeaders__KnownNetworks__0=10.42.0.0/16</code>
            
             so a real deployment can narrow it to the one proxy in front of it.
             </summary>
        </member>
        <member name="F:YDot.PAY.Api.ServiceContainer.ForwardedHeadersConfiguration.SectionName">
            <summary>Configuration section holding the CIDR list, when one is supplied.</summary>
        </member>
        <member name="F:YDot.PAY.Api.ServiceContainer.ForwardedHeadersConfiguration.DefaultKnownNetworks">
             <summary>
             Loopback plus the three RFC 1918 ranges and the IPv4-mapped forms of them.
            
             These are the addresses a reverse proxy on the same private network arrives from. They
             are never routable from the internet, so a forged header still cannot come from outside;
             what it does cover is every container network Docker, Compose and Kubernetes hand out by
             default, which is the whole point.
             </summary>
        </member>
        <member name="M:YDot.PAY.Api.ServiceContainer.ForwardedHeadersConfiguration.TryParseNetwork(System.String,Microsoft.AspNetCore.HttpOverrides.IPNetwork@)">
             <summary>
             Parses one CIDR entry.
            
             A malformed entry is skipped rather than thrown on. A typo in an environment variable
             should narrow what is trusted — the safe direction — not stop the service booting.
             </summary>
        </member>
        <member name="T:Program">
            <summary>
            Auto-generated public partial Program class for top-level statement apps.
            </summary>
        </member>
        <member name="T:StartupExtensions">
            <summary>Startup helpers, kept out of the top-level statements above.</summary>
        </member>
        <member name="M:StartupExtensions.UsePaymentSerilog(Microsoft.Extensions.Hosting.IHostBuilder)">
             <summary>
             Serilog, reading its sinks from configuration.
            
             The Seq sink is added only when a URL is configured, so a deployment without a Seq server
             does not throw at startup on a line that has nothing to do with the service's actual work.
             </summary>
        </member>
        <member name="M:StartupExtensions.InitialiseDatabaseAsync(Microsoft.AspNetCore.Builder.WebApplication)">
             <summary>
             Applies migrations.
            
             A FAILURE HERE IS RETHROWN, deliberately. A service that starts against a database it
             could not migrate will fail later, in scattered ways, at request time - far harder to
             diagnose than refusing to start. On a service that records money the difference is between
             finding out now and finding out from a donor.
            
             THERE IS NO REFERENCE DATA TO SEED HERE, unlike the other three services. Everything in
             these tables is a record of something that actually happened, and seeding a donation would
             put money in the books that nobody gave. The seeder's one job - a disabled test gateway
             account - is per-organisation and is invoked when an organisation is onboarded, not on
             every start.
             </summary>
        </member>
        <member name="M:Microsoft.AspNetCore.OpenApi.Generated.DocumentationCommentIdHelper.CreateDocumentationId(System.Type)">
            <summary>
            Generates a documentation comment ID for a type.
            Example: T:Namespace.Outer+Inner`1 becomes T:Namespace.Outer.Inner`1
            </summary>
        </member>
        <member name="M:Microsoft.AspNetCore.OpenApi.Generated.DocumentationCommentIdHelper.CreateDocumentationId(System.Reflection.PropertyInfo)">
            <summary>
            Generates a documentation comment ID for a property.
            Example: P:Namespace.ContainingType.PropertyName or for an indexer P:Namespace.ContainingType.Item(System.Int32)
            </summary>
        </member>
        <member name="M:Microsoft.AspNetCore.OpenApi.Generated.DocumentationCommentIdHelper.CreateDocumentationId(System.Type,System.String)">
            <summary>
            Generates a documentation comment ID for a property given its container type and property name.
            Example: P:Namespace.ContainingType.PropertyName
            </summary>
        </member>
        <member name="M:Microsoft.AspNetCore.OpenApi.Generated.DocumentationCommentIdHelper.CreateDocumentationId(System.Reflection.MethodInfo)">
            <summary>
            Generates a documentation comment ID for a method (or constructor).
            For example:
              M:Namespace.ContainingType.MethodName(ParamType1,ParamType2)~ReturnType
              M:Namespace.ContainingType.#ctor(ParamType)
            </summary>
        </member>
        <member name="M:Microsoft.AspNetCore.OpenApi.Generated.DocumentationCommentIdHelper.GetTypeDocId(System.Type,System.Boolean,System.Boolean)">
            <summary>
            Generates a documentation ID string for a type.
            This method handles nested types (replacing '+' with '.'),
            generic types, arrays, pointers, by-ref types, and generic parameters.
            The <paramref name="includeGenericArguments"/> flag controls whether
            constructed generic type arguments are emitted, while <paramref name="omitGenericArity"/>
            controls whether the generic arity marker (e.g. "`1") is appended.
            </summary>
        </member>
        <member name="M:Microsoft.AspNetCore.OpenApi.Generated.DocumentationCommentIdHelper.NormalizeDocId(System.String)">
            <summary>
            Normalizes a documentation comment ID to match the compiler-style format.
            Strips the return type suffix for ordinary methods but retains it for conversion operators.
            </summary>
            <param name="docId">The documentation comment ID to normalize.</param>
            <returns>The normalized documentation comment ID.</returns>
        </member>
    </members>
</doc>
