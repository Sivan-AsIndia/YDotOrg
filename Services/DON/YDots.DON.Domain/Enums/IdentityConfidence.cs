namespace YDots.DON.Domain.Enums;

/// <summary>Confidence band shown on duplicate review and identity verification.</summary>
public enum IdentityConfidence
{
    Unknown = 0,
    Low = 1,
    Medium = 2,
    High = 3
}
