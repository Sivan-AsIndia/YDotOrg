namespace YDot.IAM.Domain.Enums;

/// <summary>Whether the currency symbol is written before the amount ("$10") or after it ("10 kr").</summary>
public enum SymbolPosition
{
    Prefix = 0,
    Suffix = 1
}
