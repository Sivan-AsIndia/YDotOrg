namespace YDot.IAM.Domain.Enums;

/// <summary>Lifecycle of the root platform entity (for example ngoplanet.com).</summary>
public enum BusinessUnitStatus
{
    Draft = 0,
    Active = 1,
    Suspended = 2,
    Archived = 3
}
